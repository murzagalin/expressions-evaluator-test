pluginManagement {
    repositories {
        google()
        gradlePluginPortal()
        mavenCentral()
    }
}

rootProject.name = "expressions-evaluator-test"
include(":androidApp")
include(":shared")